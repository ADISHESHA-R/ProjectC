package com.attendance.system.enums;

/**
 * Site attendance register cell codes (paper legend). Stored when user overrides / marks register.
 */
public enum RegisterAttendanceCode {
    P,
    A,
    S,
    HQ,
    LS,
    IN
}
