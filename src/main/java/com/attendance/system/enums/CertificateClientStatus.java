package com.attendance.system.enums;

/**
 * Customer-facing certificate / feedback lifecycle for a job (site).
 */
public enum CertificateClientStatus {
    NONE,
    FEEDBACK_SUBMITTED,
    APPROVED_BY_CLIENT
}
